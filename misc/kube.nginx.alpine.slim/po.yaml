apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    app: nginx
spec:
  containers:
  - name: nginx
    image: nginx:alpine-slim
    ports:
    - containerPort: 80
    - containerPort: 443
  volumes:
  - name: nginx
    hostPath:
      path: /nginx

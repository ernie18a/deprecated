apiVersion: v1
kind: Service
metadata:
  name: redis-service
  labels:
    app: redis-cluster-app
spec:
  ports:
  - name: redis-port
    port: 6379
#   targetPort: 6380
  selector:
    app: redis-cluster-app
